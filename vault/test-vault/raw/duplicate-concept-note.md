# More on Caching (Duplicate Test)

This note intentionally overlaps with the prompt caching note.
It mentions "cache breakpoints" again and adds one fact:

- Cache breakpoints can be set at any position in the messages array,
  not just the system prompt.

This should trigger the "extend existing wiki note" path in /ingest.
